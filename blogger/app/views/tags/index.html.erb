<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag), id: "tag_#{tag.id}" %>
    </li>
  <% end %>
</ul>
<br></br>

<%= link_to "Articles List", articles_path %>
